spring.application.name=final-grupo04

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.orm.jdbc.bind=TRACE
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE

springdoc.swagger-ui.enabled=true

# Asegura que Hibernate crea las tablas antes de `data.sql`
#spring.jpa.hibernate.ddl-auto=create-drop

# Cambia a update para evitar que la base de datos se elimine en cada inicio
spring.jpa.hibernate.ddl-auto=update

# Asegura que el archivo `data.sql` se ejecute siempre
spring.sql.init.mode=always

# Retrasa la ejecución de `data.sql` hasta que Hibernate cree las tablas
spring.jpa.defer-datasource-initialization=true

# Mostrar consultas SQL en consola
logging.level.org.hibernate.SQL=DEBUG


# PostgreSQL settings
#spring.datasource.url=jdbc:postgresql://coleccionpostgres01.postgres.database.azure.com:5432/colecciondb?sslmode=require
#spring.datasource.username=user
#spring.datasource.password=Santander2025
#spring.datasource.driver-class-name=org.postgresql.Driver


# JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# Azure Blob Storage
#azure.storage.account-name=coleccionstorage
#azure.storage.account-key="clave"
#azure.storage.container-name=imagenes
#azure.storage.endpoint=https://coleccionstorage.blob.core.windows.net/

# Azure Computer Vision
#azure.vision.endpoint=https://visioncolleccion.cognitiveservices.azure.com
#azure.vision.key="clave"


# Swagger
#springdoc.swagger-ui.enabled=true

# Inicialización de datos
#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true

# Logging SQL
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.orm.jdbc.bind=TRACE
