# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mi-frontend-static
services:
  web:
    project: .
    language: js
    host: staticwebapp
    dist: dist/coleccion-app
    config:
      azureStaticWebApps:
        appLocation: .
        outputLocation: dist/coleccion-app
